
#! usr/bin/perl


use strict;
use warnings; 
use feature 'say'; 

# Perl has 3 scopes

=Start 

#1 = Global scope (our)
#2 = Lexical Scope (my) - local
#3 = Persistent scope (state) - like strict in C, retains value across multiple
                                function calls. 


You can use my as a global aswell, if it is declared outside already
=cut 